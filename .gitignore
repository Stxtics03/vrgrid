# dataset -- SemanticKITTI 00/07/08 is ~40 GB, never commit it
data/*
!data/README.md
# External KITTI dataset (C:/KITTI/dataset) - never commit
C:/KITTI/dataset/
*.bin
*.label
*.pcd
*.ply

# derived artefacts
reference_maps/
runs/
*.rrd
results/*.csv
results/*.png

# model checkpoints
*.pth
*.ckpt
*.onnx

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# editors / os
.vscode/
.idea/
.DS_Store

# Regenerated by scripts/regret_plot.py in ~40 s. Not committed: every figure
# we can currently draw is synthetic and stamped NOT REPORTABLE, and a checked-in
# PNG is exactly how a non-reportable number ends up on a slide.
docs/figures/

# Baked demo recordings from `scripts/demo.sh bake` -- ~1.2 GB of .rrd, and
# regenerable in about three minutes on any machine holding the dataset.
demo/
